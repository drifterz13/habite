<% content_for :title, "Task #{@task.id}" %>

<%= turbo_frame_tag "task_detail" do %>
  <article class="container">
    <h4 class="d-flex align-items-center gap-2">
      <%= image_tag "icons/task_scroll.png", size: "24x24", alt: "task scroll icon" %>
      <%= @task.title %>
    </h4>

    <section>
      <p><%= @task.description %></p>

      <%= button_tag "Complete quest",
      class: "btn btn-primary",
      disabled: @player.has_completed?(@task) %>

      <% if @player.has_completed? @task %>
        <p class="fw-light fs-6">
          completed at:
          <%= time_ago_in_words @player.has_completed_at(@task) %>
          ago
        </p>
      <% end %>
    </section>
  </article>
<% end %>
